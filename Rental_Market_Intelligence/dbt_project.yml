name: 'rental_market_intelligence'
version: '1.0.0'

profile: 'rental_market_intelligence'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:        
  - "target"
  - "dbt_packages"


models:
  rental_market_intelligence:
    # Config indicated by + and applies to all files under models/example/
    silver:
      +materialized: view
      +schema: silver
      +tags: ["silver"]

    gold:
      +materialized: table
      +schema: gold
      +tags: ["gold"]
